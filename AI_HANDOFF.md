# AI handoff

## Objective

Refresh the GitHub repository landing page so it accurately documents the implementation currently present in the repository, then publish the change through a draft pull request.

Current follow-up objective: let the parent choose each child CLI's model and reasoning effort, and let the dispatcher continue the same bounded task on another parent-selected CLI when the active CLI reports exhausted usage/quota. This changes child settings only, never the parent's own model or effort. Do not commit or push.

## Current state

- Branch: `codex/update-github-readme`, tracking `origin/codex/update-github-readme` in private GitHub repository `nojackno2-ctrl/agent-delegation-tools`.
- Existing tool: `codex.ps1`, a Windows wrapper around non-interactive Codex CLI execution with non-ASCII workspace junction handling.
- The repository did not previously contain a Codex skill package.
- The first `init_skill.py` attempt failed before creating files because `python` is not available on PATH; use an available Python launcher/runtime instead.
- `skills/agent-delegation-tools` is now initialized with Codex UI metadata and a bounded external-worker workflow.
- The canonical wrapper implementation moved to `skills/agent-delegation-tools/scripts/codex.ps1`; the root `codex.ps1` remains a forwarding compatibility entry point.
- Both PowerShell files pass AST parsing with zero syntax errors.
- `quick_validate.py` is currently blocked by missing `PyYAML` in the bundled Python runtime (`ModuleNotFoundError: yaml`); this is a validator dependency issue, not a reported skill validation failure.
- The validator dependency was supplied in an isolated temporary directory; `quick_validate.py` then reported `Skill is valid!`, and the temporary directory was removed.
- Direct `& .\codex.ps1` execution is blocked by the machine's PowerShell execution policy. Invoke through `powershell.exe -NoProfile -ExecutionPolicy Bypass -File` so no global policy is changed.
- Final validation passed: `quick_validate.py` reported `Skill is valid!`, both repository PowerShell entry points had zero parser errors, and `git diff --check` passed.
- Installed to `C:\Users\nojac\.codex\skills\agent-delegation-tools`; all three installed files matched source SHA-256 hashes, and the installed script parsed with zero errors.
- Claude Code 2.1.220 is currently authenticated through claude.ai; AGY 1.1.11 and the shared Antigravity bridge are currently callable.
- Attempting to invoke Claude Code with repository access was rejected before execution because the private repository contents could be transmitted to Anthropic. No Claude changes were made. Explicit user approval of private-code transmission to Anthropic and Google Antigravity is required before invoking either external agent.
- The user clarified that the canonical project checkout is `C:\離線儲存\程式設計\子代理`. The validated Codex-skill work was reconciled into that checkout from the Documents clone; all seven synchronized files matched source SHA-256 hashes.
- The user subsequently gave informed approval to continue with both external agents after the private-code transmission risk was explained.
- For the current GitHub-page refresh, branch `codex/update-github-readme` was created from the clean, synchronized `master` branch.
- `README.md` was rewritten as a focused Traditional Chinese landing page for the verified Codex skill/wrapper. It now documents prerequisites, installation and update behavior, direct usage, parameters, non-ASCII junction handling, safety boundaries, and project structure.
- The README now labels Claude Code and Antigravity integration as not yet included instead of presenting local machine tooling or pending work as released repository functionality.
- README validation passed: UTF-8 contained no replacement characters, Markdown had balanced code fences, both PowerShell entry points parsed with zero errors, `git diff --check` passed, and a temporary install/update simulation produced all 3 expected skill files without nesting the directory.
- Local commit `b2bc8e8` (`Refresh GitHub landing page`) was created. The first push attempt was rejected by the safety reviewer because explicit approval is required to transmit the README and `AI_HANDOFF.md` contents to the private GitHub repository; no remote mutation occurred.
- The user then explicitly authorized the push. Host-context `gh auth status` confirmed the active `nojackno2-ctrl` account with repository access.
- Commits `b2bc8e8` and `6261df0` were pushed to `origin/codex/update-github-readme`.
- The GitHub connector could not access the private repository and returned HTTP 404. Authenticated `gh pr create` succeeded as the documented fallback, creating draft PR #1: `https://github.com/nojackno2-ctrl/agent-delegation-tools/pull/1`.
- Codex wrapper optimization is in progress in the uncommitted worktree. The wrapper now uses path-hashed junction names and refuses to repoint mismatched aliases, resolves Codex from an explicit path/environment override/PATH/Desktop install, validates file-system and output paths, supports non-ASCII `-AddDir`, and forwards `-Ephemeral` and `-ApproveForMe`.
- Added a dependency-free Windows PowerShell integration test with a fake CLI. It directly verified non-ASCII primary/additional directory aliases, same-leaf collision avoidance, absolute output paths, reasoning-effort quoting, new flags, and success/failure exit-code propagation. The first run exposed and then corrected a PowerShell string terminator and UTF-8-without-BOM test-fixture issue; the rerun passed.
- All four PowerShell files parse with zero AST errors, the dependency-free wrapper integration test passes, and `git diff --check` passes. The test covers hashed non-ASCII primary/additional aliases, same-leaf collision avoidance, root-wrapper forwarding, drive-root path preservation, absolute output paths, reasoning-effort quoting, success/failure exit codes, and rejection of `-ApproveForMe` outside `workspace-write`.
- The first real smoke command failed before Codex launch because the test setup incorrectly used unsupported `New-Item -LiteralPath`; no test directory or product change remained. The corrected command then exposed a real resolver regression: `Get-Command codex` selected the WindowsApps package alias, whose ACL rejected direct execution even outside the sandbox. The resolver now prefers the directly launchable Desktop-managed CLI under `%LOCALAPPDATA%` and uses `PATH` only as fallback.
- The corrected real smoke test passed with Codex CLI `0.147.0-alpha.6.5`: read-only, ephemeral, non-Git isolated directory, exact final output `CODEX_WRAPPER_SMOKE_OK`, and exit code 0. The isolated directory was removed and no smoke artifacts remain.
- `quick_validate.py` reported `Skill is valid!` for both the repository and installed skill using an isolated temporary PyYAML target. The installed wrapper matches the source SHA-256 (`ABBA2AEB2C757DD6422B77959C3E507A895A8664C6EE07C7965C9F212DF7D929`) and parses with zero errors. README and both skill instruction copies document the new flags and collision-safe junction behavior. No commit or push was performed.
- Multi-backend expansion is now in progress. The installed wrappers under `C:\Users\nojac\.codex\skills\agent-delegation-tools` were inspected read-only as a design reference. The repository implementation will tighten their defaults and testability: AGY and Claude analysis default to read-only modes, CLI paths are explicitly overridable, Claude receives prompts over UTF-8 stdin with a hard timeout, all workers share a recursion guard, and the dispatcher launches exactly one selected backend.
- Added canonical `agy.ps1`, `claude.ps1`, and `delegate.ps1` scripts under the skill plus root compatibility forwarders. AGY defaults to `plan` and uses its native bounded print timeout; Claude defaults to `plan` with isolated context, UTF-8 stdin, and a 900-second wrapper timeout; the dispatcher defaults to `analysis` plus `read-only` and selects exactly one backend. `AGENT_DELEGATION_DEPTH` now guards AGY, Claude, the dispatcher, and the preserved optimized Codex wrapper against recursive delegation.
- The bounded Codex CLI implementation worker created the multi-backend draft but hit its 900-second hard timeout (exit 124), so its report was not accepted as success. Parent-run tests then found two test-harness defects rather than verified product failures: child harnesses did not exit with `$LASTEXITCODE`, and the fake Claude CLI did not encode its stdout as UTF-8. These fixtures were corrected before rerunning verification.
- Parent integration now uses one canonical skill package for all three hosts. Added a SHA-256-verifying `install.ps1` plus a temporary-root install/update test, rewrote the skill and README for AGY/Codex/Claude usage, updated UI metadata, added AGY `-AddDir` forwarding, and extended Codex recursion-guard coverage.
- All five dependency-free integration tests now pass: Codex, AGY, Claude, dispatcher, and three-host install/update. All 17 PowerShell files parse with zero AST errors; `git diff --check`, UTF-8/replacement-character checks, Markdown fence checks, metadata generation, and `quick_validate.py` (`Skill is valid!`) pass.
- Ran `install.ps1 -Target All`. The canonical six-file package was synchronized to `.codex\skills`, `.agents\skills`, and `.claude\skills`; parent verification confirmed all 18 installed file hashes match source and all 12 installed wrapper parses have zero errors.
- Real direct wrapper smokes passed in a temporary non-Git directory: AGY 1.1.11 plan mode returned exact `AGY_WRAPPER_SMOKE_OK`; Codex CLI 0.147.0-alpha.6.5 read-only/ephemeral returned exact final message `CODEX_WRAPPER_SMOKE_OK`; Claude Code 2.1.220 isolated plan returned exact `CLAUDE_WRAPPER_SMOKE_OK` with JSON `is_error=false`, one turn, and exit 0.
- Host-level forward tests also passed. An outer Antigravity session discovered `$agent-delegation-tools`, launched one AGY child in plan mode, and returned `AGY_HOST_CHILD_OK` with exit 0. An outer Claude session discovered the installed skill, launched one isolated plan-mode Claude child, and returned exactly `CLAUDE_HOST_CHILD_OK` with exit 0. The current Codex host launched the Codex CLI direct wrapper and received the successful Codex sentinel above.
- Final completion audit passed without narrowing the objective: 5/5 integration suites, 17/17 PowerShell AST parses, skill validation, 18/18 installed source/hash comparisons, all three current CLI version checks, all direct smoke sentinels, and both external host-level child sentinels. The smoke directory was removed. Git HEAD remains `5c7768f`; all task changes are uncommitted and nothing was pushed.
- New objective audit: the three direct wrappers already forward child `-Model` and `-Effort`, but `delegate.ps1` has only one shared pair, launches exactly one backend, and has no quota-exhaustion detection or cross-CLI continuation. The dispatcher therefore needs parent-ordered fallback candidates plus per-backend child model/effort values while preserving current single-backend calls.
- Implemented the new dispatcher behavior in the canonical skill script. The parent can set `AgyModel/AgyEffort`, `CodexModel/CodexEffort`, and `ClaudeModel/ClaudeEffort`, while legacy `Model/Effort` applies only to the primary child. `FallbackAgent` is an explicit ordered chain; attempts remain sequential, ordinary failures stop immediately, recognized quota/usage/rate-limit/credit/429 failures continue, and an entirely exhausted chain returns exit 75.
- Cross-CLI continuation preserves the original task and current worktree, includes at most `HandoffMaxChars` of prior output as untrusted progress notes, and never changes the parent model or effort. The first expanded dispatcher test passed, including per-provider child settings, non-quota stop behavior, AGY-to-Claude continuation, Claude zero-exit JSON quota detection, final output copying, and exit 75 when all candidates are exhausted.
- Updated `SKILL.md`, `README.md`, and OpenAI skill metadata to teach parent-side child model/effort selection, explicit cross-provider authorization, opt-in fallback order, recognized quota conditions, sequential execution, continuation semantics, and exit 75. Full regression and install synchronization are still pending.
- A new Claude-to-Codex fallback assertion initially failed because the fake Codex CLI records a multiline prompt across multiple text lines and the test inspected only the final line. Backend launch and child settings were correct; the assertion was changed to inspect the complete UTF-8 argument record before rerunning.
- Full regression passed after the assertion fix: all five dependency-free suites (AGY, Claude, Codex, dispatcher, and installer), all 17 PowerShell AST parses, `git diff --check`, UTF-8 replacement-character checks, and balanced Markdown fences. Dispatcher coverage now includes AGY-to-Claude and Claude-to-Codex quota continuation, all three provider-specific child model/effort pairs, ordinary-error stop behavior, bounded handoff content, terminal output copying, and exit 75 when all candidates are exhausted.
- Applied the `skill-creator` workflow for this existing skill update. Deterministic `generate_openai_yaml.py` regenerated the three UI fields, and `quick_validate.py` reported `Skill is valid!` using PyYAML installed only into a verified temporary directory; that directory was removed afterward.
- Before installation, exactly the expected three files differed in each host copy: `SKILL.md`, `agents/openai.yaml`, and `scripts/delegate.ps1`. `install.ps1 -Target All` then synchronized the canonical package successfully to `.codex\skills`, `.agents\skills`, and `.claude\skills`. Post-install hash/parse verification is pending.
- Post-install verification passed: all 18 installed source/hash comparisons match and all 12 installed wrapper AST parses have zero errors. Current local CLIs remain AGY 1.1.11, Codex CLI 0.147.0-alpha.6.5, and Claude Code 2.1.220; current help/catalog output confirms their child model/effort interfaces.
- A real dispatcher smoke in an empty temporary directory launched AGY with the parent-selected `gemini-3.6-flash-low` model and `low` effort, returned `AGY_CHILD_CONFIG_OK`, and exited 0. AGY's first-use model preparation also emitted a serialized PowerShell progress record (`#< CLIXML`) into its captured output; the dispatcher harness now sets `ProgressPreference=SilentlyContinue` to prevent wrapper-host progress from polluting child results, pending rerun.
- An independent read-only forward-test found no blocking defect and independently reran all five test suites, AST parsing, diff checks, and installed hashes. It confirmed per-provider child settings, sequential ordered fallback, ordinary-error stop behavior, and exit 75. It correctly noted that real quota exhaustion is not being induced, zero-exit unstructured text errors intentionally do not trigger fallback, and no single test yet traverses all three candidates in one chain.
- The AGY dispatcher smoke rerun passed with exact `AGY_CHILD_CONFIG_OK`, exit 0, and no CLIXML after suppressing progress in the isolated harness.
- A real Codex dispatcher smoke supplied `gpt-5.6-terra` plus `low`; the CLI startup banner confirmed that child model and effort, then the live account returned `You've hit your usage limit`. With no fallback candidates, the dispatcher correctly returned exit 75. This provides real quota evidence rather than an induced/simulated exhaustion.
- The first real `Codex -> Claude -> AGY` fallback command failed before launching a CLI because `powershell.exe -File` bound `claude,agy` as one string and `ValidateSet` rejected it. The root compatibility wrapper then incorrectly returned exit 0 for that binding error. The canonical dispatcher now normalizes both comma-separated values and programmatic arrays before manual validation, and all four root wrappers use terminating error handling so launch/binding failures return nonzero. Tests and the real fallback rerun are pending.
- Added cumulative bounded handoff history so a third candidate retains usable notes from both earlier attempts instead of receiving only the immediately preceding quota error. The root dispatcher integration test now traverses all three fake candidates (`AGY -> Claude -> Codex`), verifies the first and second outputs both reach the third child, accepts comma-separated fallback values, and rejects invalid values before launching a backend.
- Real cross-CLI continuation passed in an empty temporary directory. Attempt 1 launched Codex with `gpt-5.6-terra`/`low`, detected the live usage-limit error, and emitted the fallback warning; attempt 2 launched Claude with `sonnet`/`low` and returned exact `REAL_CROSS_CLI_FALLBACK_OK`. Overall exit was 0 and AGY attempt 3 did not run. The sentinel-only temporary directory was verified under the system temp parent and deleted.
- Final regression after the interface/error-propagation fixes passed: all five suites, 17/17 repository PowerShell parses, `git diff --check`, and explicit invalid-parameter checks proving all four root wrappers now return exit 1 instead of swallowing binding errors. The canonical package was reinstalled to all three host skill directories; 18/18 installed hashes match and 12/12 installed wrapper parses have zero errors.
- 2026-08-12 cross-project optimization audit: the separate `AI倒數喚醒` thread exposed three production gaps in this skill. AGY write mode initially stalled on headless command approval until `-SkipPermissions` was used; AGY, Codex, and Claude then each reached an outer 184-second timeout without delivering code; Codex also left a temporary schema directory after timeout. The current dispatcher does not expose AGY's skip-permissions switch and does not bound every backend process.
- The same external project directly validated Codex app-server `account/rateLimits/read`: strict `initialize` response ordering is required before `initialized`, stdin must be UTF-8 without BOM, and the response provides `usedPercent`, `windowDurationMins`, and `resetsAt`. AGY 1.1.11 and Claude Code 2.1.220 help still expose no equivalent machine-readable account-usage query, so their usage state must remain explicitly unsupported rather than estimated.
- Current local CLI evidence is Codex 0.147.0-alpha.6.6, AGY 1.1.11, and Claude Code 2.1.220. Claude now documents `--prompt-suggestions false` and `--no-session-persistence` for print mode; the cross-project implementation already uses the former to avoid unnecessary suggestion output. Official Codex developer commands recommend `--sandbox workspace-write` for unattended local work, while the local help still advertises `--approve-for-me`; an earlier project invocation rejected that combination, so it must remain an optional, compatibility-sensitive flag rather than an assumed default.
- Planned skill changes from the audit: add a deterministic Codex usage-status helper with explicit unsupported results for AGY/Claude, add bounded dispatcher termination with exit 124 and partial-output preservation, forward an explicitly authorized AGY skip-permissions option, disable Claude prompt suggestions and persistence for isolated one-shot work, and add regression tests for these behaviors. Do not auto-route from guessed provider usage or auto-delete child-created worktree artifacts.
- Implemented the cross-project improvements in the canonical package. Added `scripts/status.ps1` plus a root forwarder; it performs a strict Codex app-server handshake using UTF-8 without BOM, returns rate-limit windows and reset times, and reports AGY/Claude as unsupported without estimating values. Codex direct delegation and every dispatcher attempt now have wall-clock process-tree bounds with exit 124; dispatcher preserves captured timeout evidence and does not fallback on timeout. Dispatcher also exposes `-AgySkipPermissions` only with explicit `workspace-write`. Claude one-shot runs now disable prompt suggestions and session persistence by default.
- The first new status test exposed a Windows PowerShell 5.1 `Generic.List` expansion failure (`Argument types do not match`) in `status.ps1`; replacing `@($genericList)` with explicit pipeline-to-object-array conversion fixed it. The focused status, Codex, Claude, and dispatcher suites then passed; Codex and dispatcher timeout tests terminated fake process trees and returned 124. Full regression, metadata validation, live status verification, and installed-copy synchronization remain pending.
- The user expanded the objective to require simultaneous multi-child operation with end-of-task verification. Added canonical `scripts/parallel.ps1` plus a root compatibility forwarder. It reads a UTF-8 JSON task manifest, launches up to `MaxConcurrency` independent dispatcher processes, waits at a completion barrier, isolates each task's final/raw/stdout/stderr/result artifacts, and writes one ordered `summary.json`.
- Parallel write safety is explicit: `danger-full-access` is rejected, every `workspace-write` task requires relative `writeScope` values, cross-task overlapping scopes are rejected before any child starts, and scope boundaries are injected into each write prompt. Dependent work must be split into later waves; children do not communicate directly.
- Added `parallel.Tests.ps1` and a fake delegate. The focused suite directly proves two workers overlap by requiring a shared start barrier, preserves task-file order and isolated outputs, aggregates mixed success/failure while retaining exit 23, returns exit 124 and preserves process-tree timeout evidence, rejects overlapping write scopes, and verifies the root forwarder. The first timeout assertion exposed a single-object JSON array normalization bug; wrapping the conditional JSON result in an outer array fixed it. The focused suite now passes. Full regression and install synchronization are pending.
- Full dependency-free regression passed with all seven suites: AGY, Claude, Codex, dispatcher, installer, parallel batch, and status. All 25 repository PowerShell files parse; `git diff --check`, the 30-file UTF-8 replacement-character scan, balanced README/SKILL fences, deterministic OpenAI metadata generation, and `quick_validate.py` (`Skill is valid!`) pass.
- Installed the eight-file canonical package to Codex, Antigravity, and Claude. All 24 installed source/hash comparisons match and all 18 installed scripts parse. The parallel suite also passed when its canonical entry was overridden to the installed Codex copy, directly verifying the installed `parallel.ps1` concurrency barrier, outputs, failures, timeout, scope rejection, and root forwarding behavior.
- A live read-only call through the installed Codex `status.ps1` succeeded without a model turn: the current window reported 21% used, 79% remaining, 10080 minutes, and reset `2026-08-18T02:36:18Z`; AGY and Claude remained explicitly unsupported rather than estimated.
- Final post-refinement audit passed: all seven suites, `Skill is valid!`, 25/25 repository PowerShell parses, `git diff --check`, 24/24 installed source/hash comparisons, and 18/18 installed script parses. The installed-copy parallel suite also passed. All current changes remain uncommitted and unpushed.
- Clarified the child configuration contract for the current objective: an explicit user provider-specific model/effort wins, then explicit primary `Model/Effort`, then a parent-selected value when the user leaves the field open. The dispatcher intentionally has no hard-coded model catalog; parent auto-selection is materialized through the existing provider-specific parameters, while `Agent=auto` continues to select the backend from `TaskType`. README and SKILL.md now document this precedence, and the dispatcher suite has a new combined assertion for user-selected `Model/Effort` surviving automatic implementation routing. Verification is pending.
- The focused dispatcher suite passed with the new combined assertion. The first attempted seven-suite regression was invalid because its outer shell timeout was accidentally set too short and terminated after about 3.6 seconds; this is a harness invocation error, not a product-test result. Rerun the complete regression with a sufficient outer timeout.
- The rerun passed all seven dependency-free suites. The first deterministic `generate_openai_yaml.py` attempt for refreshed UI metadata did not run because the discovered Python executables are broken uv trampolines (`uv trampoline failed to spawn Python child process`); the YAML remained unchanged. Locate a working runtime and rerun rather than editing generated metadata manually.
- Found the actual uv-managed CPython runtime at `C:\Users\nojac\AppData\Local\Python\pythoncore-3.14-64\python.exe`. Deterministic metadata generation then succeeded with UI text covering child model choice and automatic selection, and `quick_validate.py` reported `Skill is valid!`. Installed-copy synchronization and final repository checks remain pending.
- `install.ps1 -Target All` synchronized the updated canonical package to Codex, Antigravity, and Claude successfully. Final independent hash/parse/diff checks remain pending.
- Final verification for user-configurable child model/effort plus parent automatic selection passed: all seven suites, `Skill is valid!`, 25/25 repository PowerShell parses, `git diff --check`, UTF-8/replacement-character and Markdown fence checks, 24/24 installed source/hash comparisons, and 18/18 installed script parses. The updated package is live in all three host skill directories. Changes remain uncommitted and unpushed.
- Follow-up behavior change: dispatcher `Sandbox` is now derived from `TaskType` when omitted (`implementation`/`scaffolding` => `workspace-write`; `analysis`/`review` => `read-only`) consistently for AGY, Codex, and Claude. Direct wrappers now stop before task launch when Codex or Claude login status is unauthenticated, return exit `78`, and print the interactive login command; AGY detects authentication failures from its CLI output and returns the same actionable status.
- Windows PowerShell 5.1 compatibility fixes: dispatcher and parallel launcher no longer rely on a null `ProcessStartInfo.Environment` dictionary; they temporarily set inherited environment variables around process start and restore them. Source tests cover all three wrappers, dispatcher, and parallel batches; the first parallel run exposed this runtime issue and the corrected rerun passed.
- Verification after the follow-up change: all five source suites passed, including login-required and automatic workspace-write assertions; canonical package synchronization to Codex, Antigravity, and Claude succeeded with elevated file access. Full installed hash/parse audit and final documentation checks remain pending.
- Final follow-up verification passed: all seven dependency-free suites passed (agy, claude, codex, delegate, install, parallel, status); 13 repository PowerShell entry/scripts parsed with zero errors; `git diff --check` passed; README UTF-8 has no replacement characters and balanced Markdown fences; skill `quick_validate.py` reported `Skill is valid!`; all 24 source files matched SHA-256 across the Codex, Antigravity, and Claude installed copies. Changes remain uncommitted and unpushed.
- Real Codex login preflight through the root wrapper returned exit `78` before model launch with actionable `codex login` guidance. The temporary preflight directory was removed.
- Sandbox compatibility follow-up: the current Codex Desktop install exposes an authenticated CLI at `%USERPROFILE%\.codex\.sandbox-bin\codex.exe`, while PATH resolves to a WindowsApps binary whose package ACL returns `Access is denied`. The canonical Codex worker and status helper now prefer `CODEX_HOME\.sandbox-bin\codex.exe` and then the user-profile sandbox copy before Desktop/PATH. Resolver regression assertions cover both scripts and both sandbox locations. All seven suites, 25/25 repository PowerShell parses, `git diff --check`, deterministic UI metadata generation, and `quick_validate.py` (`Skill is valid!`) pass. Synchronization to all three hosts passes with 24/24 installed source hashes and 18/18 installed script parses.
- Codex App tool sandboxes run as `CodexSandboxOffline`, so host CLI authentication remains intentionally unavailable to a direct nested process even though both identities see the same `auth.json` hash. The worker and status helper now explain this boundary, return safe approved-host-execution guidance, and explicitly forbid copying credentials into workspace/temp. A corrected live invocation from the sandbox workflow through approved host execution used default path resolution, launched Codex CLI `0.147.0-alpha.6.6` in read-only ephemeral mode, returned exact `SANDBOX_SKILL_OK`, and exited 0; its temporary directory was removed. The earlier `-RawFile` failure was only an unsupported test-harness argument.

## Constraints

- Keep the root `codex.ps1` command compatible with documented usage.
- Default delegated Codex execution to `workspace-write`; use `read-only` for analysis.
- Avoid recursive or unbounded delegation. Single-task fallback remains sequential; parallel batches require an explicit finite task manifest, concurrency cap, synchronization barrier, and non-overlapping write ownership.
- Do not commit or push without explicit authorization.

## Next steps

- Review draft PR #1 and merge it when the README wording is accepted.
- Keep the current changes uncommitted and unpushed until the user explicitly authorizes publication.
