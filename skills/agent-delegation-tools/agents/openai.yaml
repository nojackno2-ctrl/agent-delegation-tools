interface:
  display_name: "Agent Delegation Tools"
  short_description: "Run sandbox-safe bounded CLI workers."
  default_prompt: "Use $agent-delegation-tools to run a bounded CLI worker safely from the Windows sandbox."
