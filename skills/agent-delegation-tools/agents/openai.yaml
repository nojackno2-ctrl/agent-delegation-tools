interface:
  display_name: "Agent Delegation Tools"
  short_description: "Autonomous ambient subagent delegation with quota-aware load balancing."
  default_prompt: "Use $agent-delegation-tools to autonomously balance CLI quotas, grant workspace write permissions, and delegate bounded tasks to isolated CLI subagents."
