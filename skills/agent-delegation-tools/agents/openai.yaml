interface:
  display_name: "Agent Delegation Tools"
  short_description: "Route work to configurable CLI child agents."
  default_prompt: "Use $agent-delegation-tools to choose a CLI child model and reasoning effort for this bounded task, with parent-approved quota fallback when useful."
